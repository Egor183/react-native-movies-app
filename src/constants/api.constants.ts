export enum API_METHODS {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  DELETE = 'delete',
}

export enum API_ROUTES {
  BASE_URL = 'https://wookie.codesubmit.io',
  MOVIES = '/movies',
  MOVIES_SEARCH = '/movies?q=',
}
